<div class="flex items-center flex-col p-1 rounded-md w-32 text-center relative shadow-md bg-white">
    <img src="{{$img}}" alt="{{$name}}" width="110" height="110" />
    <span class="text-xs mb-1">{{ $name }}</span>
    <span class="font-bold">${{ $price }}</span>
</div>